{
  "private": true,
  "version": "0.4.1",
  "scripts": {
    "release": "lerna publish"
  },
  "gitHooks": {
    "pre-commit": "lint-staged"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write"
    ],
    "*.ts": [
      "prettier --parser=typescript --write"
    ]
  },
  "devDependencies": {
    "@ls-lint/ls-lint": "^1.9.2",
    "lerna": "^3.22.1",
    "lint-staged": "^10.2.11",
    "prettier": "^2.0.5",
    "typescript": "^4.0.2",
    "yorkie": "^2.0.0"
  }
}
